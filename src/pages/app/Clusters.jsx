import { Typography, Container } from '@mui/material';

    function Clusters() {
      return (
        <Container>
          <Typography variant="h4" gutterBottom>
            k3s Clusters
          </Typography>
          <Typography>
            Manage your k3s clusters here
          </Typography>
        </Container>
      );
    }

    export default Clusters;
