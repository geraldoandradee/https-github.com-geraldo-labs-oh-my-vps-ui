import { Typography, Container } from '@mui/material';

    function Servers() {
      return (
        <Container>
          <Typography variant="h4" gutterBottom>
            Servers
          </Typography>
          <Typography>
            Manage your VPS instances here
          </Typography>
        </Container>
      );
    }

    export default Servers;
